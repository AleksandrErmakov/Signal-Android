<?xml version="1.0" encoding="utf-8"?>

<PreferenceScreen   xmlns:android="http://schemas.android.com/apk/res/android"
                    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ListPreference android:key="pref_theme"
                    android:title="@string/preferences__theme"
                    android:entries="@array/pref_theme_entries"
                    android:entryValues="@array/pref_theme_values"
                    android:defaultValue="light"/>

    <ListPreference android:key="pref_language"
                    android:title="@string/preferences__language"
                    android:entries="@array/language_entries"
                    android:entryValues="@array/language_values"
                    android:defaultValue="zz"/>

    <!--<ListPreference android:key="pref_color"
                    android:title="@string/preferences__color"
                    android:defaultValue="@android:color/black"
                    android:negativeButtonText="@null"
                    android:positiveButtonText="@null"
                    android:persistent="false"
                    app:numColumns="5"/>-->

    <!--<org.thoughtcrime.securesms.preferences.ColorBackgroundPreference
        android:key="pref_color"
        android:title="@string/preferences__color"
        android:summary="@string/recipient_preferences__color"
        android:defaultValue="@android:color/signal_primary"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:persistent="false"
        app:numColumns="5" />-->

    <org.thoughtcrime.securesms.preferences.ColorBackgroundPreference
        android:key="pref_color"
        android:title="@string/preferences__color"
        android:summary="@string/preferences__color_summary"
        android:defaultValue="@color/signal_primary"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:persistent="false"
        app:numColumns="5" />

</PreferenceScreen>
